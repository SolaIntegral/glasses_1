# MVP機能実装チェックリスト

## 📊 全体進捗
- 実装済み: 17/18 必須項目
- 欲しい機能: ほぼ実装済み
- 未実装: 1 項目のみ（写真アップロード）

---

## ✅ 絶対必須機能

### 🔹 サポーター（講師）用

#### 1. 登録、ログイン・ログアウト
- [x] ログイン機能
- [x] ログアウト機能
- [x] セッション管理（localStorage）
- **備考**: 管理者がアカウントを作成する方式

#### 2. プロフィール登録画面
- [x] 名前（ニックネーム可）表示
- [x] 名前の編集機能（管理者のみ実装済み）
- [x] **面談用URL** ✅ 実装済み
- [x] 写真表示
- [ ] 写真アップロード機能（未実装）
- [x] **性別** ✅ 実装済み
- [x] **所属** ✅ 実装済み
  - [x] 業界（実装済み）
  - [x] 業種（実装済み）
  - [x] 職種（実装済み）
- [x] **経歴** ✅ 実装済み
  - [x] 学歴（実装済み）
  - [x] 過去の業界、業種、職種（実装済み）
- [x] **趣味** ✅ 実装済み
- [x] **高校時所属していた部活（あれば）** ✅ 実装済み
- [x] **高校生へひとこと（100文字程度）** ✅ 実装済み

**現在の実装状況**:
- ✅ 講師名、自己紹介（bio）、専門分野（specialties）
- ✅ 面談用URL、性別、所属（業界・業種・職種）
- ✅ 経歴（学歴、過去の職歴）
- ✅ 趣味、高校時の部活、高校生へのメッセージ
- ❌ 写真アップロード機能のみ未実装

#### 3. 通報機能
- [x] Google Formへのリンク埋め込み ✅ 実装済み
- [x] 運営へのフォーム送信機能 ✅ 実装済み
- **備考**: 通報ページを作成し、ヘッダーにリンクを追加

#### 4. 空き時間枠登録（30分一枠）
- [x] 空き時間枠登録機能
- [x] 空き時間一覧表示
- **備考**: 30分枠は実装されているか要確認

#### 5. 高校生からの予約一覧
- [x] 予約一覧表示
- [x] 予約してきた高校生のプロフィール画面へのリンク
- [x] **定例か単発かが分かる表示** ✅ 実装済み

**現在の実装**: 講師は予約一覧を閲覧可能、MTGリンクも表示、単発/定例の区別も表示

---

### 🔹 高校生（生徒）用

#### 1. 登録、ログイン・ログアウト
- [x] ログイン機能
- [x] ログアウト機能
- [x] セッション管理（localStorage）
- **備考**: 管理者がアカウントを作成する方式

#### 2. プロフィール登録画面
- [x] ニックネーム（本名×）表示
- [x] **学年** ✅ 実装済み
- [x] **性別** ✅ 実装済み
- [x] **部活（あれば）** ✅ 実装済み
- [x] **趣味、好きなこと** ✅ 実装済み

**現在の実装状況**:
- ✅ ニックネーム（displayName）
- ✅ 学年、性別、部活、趣味、好きなこと
- ✅ 関心事・興味分野（interests）※管理者のみ編集可能

#### 3. サポーターの写真と名前一覧
- [x] サポーター一覧画面
- [x] サポーターの顔と名前表示
- [x] 所属表示 ✅ 実装済み
- [x] 一覧からサポーターをクリックすると詳細画面に遷移
- [x] プロフィール画面に遷移

#### 4. セッション予約
- [x] サポーターの空き時間枠を選んでセッション予約
- [x] **単発か定例のいずれかにチェック** ✅ 実装済み

**現在の実装**: 予約機能は動作し、単発/定例の区別を選択可能

---

## 🔹 欲しい機能（優先順位別）

### サポーター用

#### 1. 単発の場合、高校生が事前に記入した聞きたいことリストを閲覧
- [x] 単発/定例の区別機能 ✅ 実装済み
- [x] 聞きたいことリスト入力機能 ✅ 実装済み（相談したいこととして実装）
- [x] 講師が閲覧する機能 ✅ 実装済み（予約一覧に表示）
- **備考**: 単発の場合は相談したいこととして保存、講師が閲覧可能

#### 2. 動画アップロード機能
- [ ] 動画アップロードUI
- [ ] 動画ストレージ（Firebase Storage）
- [ ] 動画再生機能
- **備考**: 完全未実装

---

### 高校生用

#### 1. 単発にチェックを入れた場合の聞きたいことリスト入力
- [x] 単発/定例の区別機能 ✅ 実装済み
- [ ] Google Formへのリンク埋め込み（不要、プラットフォーム上で実装済み）
- [x] プラットフォーム上での入力機能（UI） ✅ 実装済み
- [x] サポーターへの通知機能 ✅ 実装済み（既存の通知機能を使用）
- **備考**: プラットフォーム上で入力・通知済み

---

## 📋 実装済みの機能（追加実装）

### Slack通知機能
- [x] 予約確定時の通知
- [x] 予約キャンセル時の通知
- [x] GAS経由でのSlack通知送信
- [ ] セッション前リマインダー（GASトリガー未実装）
- [ ] セッション後レポート催促（GASトリガー未実装）

### セッション後レポート機能
- [x] 生徒向けGoogle Formへのリンク
- [x] 講師向けGoogle Formへのリンク
- [ ] 自動リマインダー送信（未実装）

### 会議リンク機能
- [x] Google Meetリンク自動設定
- [x] 予約完了画面からの参加ボタン
- [x] 講師予約一覧からの参加ボタン

---

## 🎯 優先度別実装計画

### 🔴 最優先（MVP必須機能の不足部分）

1. **講師プロフィールの拡張** ⚠️
   - 性別、所属（業界・業種・職種）、経歴（学歴、過去の職歴）、趣味、部活、高校生へひとこと
   - 写真アップロード機能

2. **生徒プロフィールの拡張** ⚠️
   - 学年、性別、部活、趣味/好きなこと

3. **単発/定例の区別機能** ⚠️
   - 予約時に単発か定例かを選択
   - 予約一覧に表示
   - 講師が確認できる

4. **通報機能** ⚠️
   - Google Formへのリンクを配置

### 🟡 次優先（欲しい機能）

1. **単発予約時の聞きたいことリスト入力** 🔵
   - 単発選択時にフォーム表示
   - Google Formリンクまたはプラットフォーム上で入力
   - 講師が閲覧できる

2. **動画アップロード機能** 🔵
   - 講師が動画をアップロード
   - 生徒が閲覧

### 🟢 オプション（追加機能）

1. セッション前リマインダー（GASトリガー実装）
2. セッション後自動催促（GASトリガー実装）
3. その他

---

## 📝 データ構造の変更が必要な項目

### Instructor型の拡張
```typescript
export interface Instructor {
  id: string;
  userId: string;
  profileImageUrl: string;
  bio: string;
  specialties: string[];
  slackWebhookUrl?: string;
  slackMemberId?: string;
  isActive: boolean;
  // MVP要件で追加が必要な項目
  meetingUrl?: string; // 面談用URL
  gender?: string; // 性別
  industry?: string; // 業界
  occupation?: string; // 業種
  jobTitle?: string; // 職種
  education?: Education[]; // 学歴
  workHistory?: WorkHistory[]; // 経歴
  hobbies?: string[]; // 趣味
  highSchoolClub?: string; // 高校時所属していた部活
  messageToStudents?: string; // 高校生へひとこと
  createdAt: Date;
  updatedAt: Date;
}
```

### StudentProfile型の拡張
```typescript
export interface StudentProfile {
  id: string;
  userId: string;
  interests: string[];
  previousConversations: ConversationHistory[];
  notes?: string;
  // MVP要件で追加が必要な項目
  grade?: string; // 学年
  gender?: string; // 性別
  club?: string; // 部活
  hobbies?: string[]; // 趣味、好きなこと
  createdAt: Date;
  updatedAt: Date;
}
```

### Booking型の拡張
```typescript
export interface Booking {
  id: string;
  instructorId: string;
  studentId: string;
  slotId: string;
  startTime: Date;
  endTime: Date;
  purpose: string;
  notes?: string;
  status: BookingStatus;
  createdAt: Date;
  updatedAt: Date;
  cancelledAt?: Date;
  cancelReason?: string;
  meetingUrl?: string;
  consultationText?: string;
  // MVP要件で追加が必要な項目
  sessionType?: 'one-time' | 'recurring'; // 単発 or 定例
  questionsBeforeSession?: string[]; // 事前に記入した聞きたいことリスト
  createdAt: Date;
  updatedAt: Date;
}
```

---

## 🚀 次のステップ

### ✅ 完了した項目
1. ✅ データ構造の拡張（型定義の更新）
2. ✅ 講師プロフィールの拡張実装
3. ✅ 生徒プロフィールの拡張実装
4. ✅ 単発/定例区別機能の実装
5. ✅ 通報機能の実装
6. ✅ 単発予約時の聞きたいことリスト入力機能

### 🔄 残りの項目
1. 動画アップロード機能（オプション）

### 📋 現在のステップ
1. **テスト実施** - `TEST_CHECKLIST.md` を参照
2. バグ修正とフィードバック対応
3. 本番運用に向けた最終調整

---

## 📅 進捗管理

- 最終更新: 2024年12月20日
- 実装済み: 17/18 必須項目
- 進捗率: 94%

